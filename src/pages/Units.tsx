import { Layout } from "@/components/layout/Layout";
import { UnitsSection } from "@/components/sections/UnitsSection";

export default function Units() {
  return (
    <Layout>
      <UnitsSection showHero />
    </Layout>
  );
}
